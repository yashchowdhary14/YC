'use client';

// Placeholder for PostsGrid component
export default function PostsGrid({ posts }: { posts: any[] }) {
  return <div className="p-4 border rounded-lg bg-card">PostsGrid with {posts.length} posts</div>;
}

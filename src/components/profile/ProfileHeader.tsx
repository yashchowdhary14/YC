'use client';

// Placeholder for ProfileHeader component
export default function ProfileHeader({ user }: { user: any }) {
  return <div className="p-4 border rounded-lg bg-card">ProfileHeader for {user.username}</div>;
}
